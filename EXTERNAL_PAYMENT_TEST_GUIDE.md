# 外部支付接口测试指南

## 控制台输出说明

当用户创建订单时，系统会在控制台输出详细的外部支付接口调用信息，包括：

### 1. 请求信息输出
```
=== 外部支付请求详情 ===
请求URL: https://therianclouds.mynatapp.cc/api/payment/create
请求方法: POST
请求头: {
  "Content-Type": "application/json"
}
请求体: {
  "openid": "用户认证token",
  "amount": 订单总价,
  "description": "水晶手串订单 - 主订单ID",
  "orderId": "6位唯一订单ID"
}
请求参数说明:
- openid: 用户认证token，从本地存储获取
- amount: 订单总价，值为: 具体金额
- description: 商品描述，值为: 水晶手串订单 - 主订单ID
- orderId: 6位唯一订单ID，值为: 生成的6位ID
```

### 2. 成功响应输出
```
=== 外部支付订单创建成功 ===
外部订单ID: 生成的6位ID
支付响应原始数据: {
  "success": true/false,
  "message": "响应消息",
  "data": 响应数据对象
}
支付响应详细信息:
- success: 请求是否成功
- message: 服务器返回的消息
- data: 服务器返回的数据对象
```

### 3. 错误响应输出
```
=== 创建外部支付订单失败 ===
错误信息: 错误对象
错误详情:
- 错误类型: 错误类型名称
- 错误消息: 具体错误消息
- 错误堆栈: 错误堆栈信息
- 响应状态: HTTP状态码（如果有）
- 响应数据: 服务器返回的错误数据（如果有）
=== 主订单创建仍然成功，继续流程 ===
```

## 如何查看控制台输出

### 微信开发者工具
1. 打开微信开发者工具
2. 加载项目后，点击底部菜单栏的"控制台"选项卡
3. 在控制台中可以查看所有日志输出

### 真机调试
1. 在真机上打开小程序
2. 使用微信开发者工具的"真机调试"功能
3. 在开发者工具的控制台中查看输出

### 重要说明
- 外部支付接口调用失败不会影响主订单的创建
- 所有外部支付相关的日志都会有明确的标识（=== 外部支付 ===）
- 请求和响应数据都会以格式化的JSON形式输出，便于调试

## 🔍 测试建议

### 1. 环境配置检查
在测试前，请确保完成以下配置：

#### 微信开发者工具设置
- ✅ 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"
- ✅ 确保开发服务器正常运行 (`npm run dev:weapp`)

#### 域名白名单配置
- ⚠️ 如果请求 `https://therianclouds.mynatapp.cc` 失败，需要在微信公众平台配置服务器域名
- 🔧 开发环境可暂时关闭域名校验，生产环境必须配置合法域名

### 2. 正常流程测试
- ✅ 确保已登录
- ✅ 购物车中有商品
- ✅ 填写完整收货地址
- ✅ 点击提交订单
- ✅ 观察控制台输出

### 3. 异常情况测试
- ❌ 未登录状态测试
- ❌ 空购物车测试
- ❌ 网络超时测试（可关闭网络模拟）
- ❌ 错误响应测试