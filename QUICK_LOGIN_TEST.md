# 快速测试登录功能

## 启动项目

```bash
npm run dev:weapp
```

## 测试场景

### 场景 1：未登录用户
1. 打开微信开发者工具
2. 清除缓存（右上角 -> 清除缓存 -> 清除全部缓存）
3. 重新编译
4. **预期结果**：自动进入"我的"页面（登录页）

### 场景 2：登录流程
1. 在"我的"页面：
   - 点击头像选择头像
   - 输入昵称
   - 点击"获取手机号"
2. **预期结果**：
   - 显示"登录成功"
   - 2秒后自动跳转到"DIY设计"页面

### 场景 3：已登录用户
1. 确保已登录
2. 关闭小程序
3. 重新打开
4. **预期结果**：自动进入"DIY设计"页面

### 场景 4：未登录访问受保护页面
1. 退出登录
2. 点击底部 TabBar 的"DIY设计"或"购物车"
3. **预期结果**：
   - 显示对话框："访问此页面需要先登录，是否前往登录？"
   - 点击"去登录"：跳转到"我的"页面
   - 点击"取消"：返回上一页或跳转到"我的"页面

### 场景 5：操作时的登录检查
1. 退出登录
2. 通过其他方式进入 DIY 页面（如果可能）
3. 点击"加入购物车"或"保存设计"
4. **预期结果**：
   - 显示对话框："加入购物车需要先登录，是否前往登录？"
   - 点击"去登录"跳转到登录页

## 常见问题

### Q: 编译报错 "Unexpected token"
A: 已修复，确保使用最新的 src/app.ts 文件

### Q: 登录后没有自动跳转
A: 检查控制台日志，确认登录是否成功

### Q: 点击 TabBar 没有跳转到登录页
A: 检查是否已经登录，已登录用户可以正常访问

### Q: 页面一直在跳转循环
A: 清除缓存后重试，或检查 token 是否有效

## 调试技巧

### 查看登录状态
在控制台输入：
```javascript
Taro.getStorageSync('token')
```

### 清除登录状态
在控制台输入：
```javascript
Taro.removeStorageSync('token')
Taro.removeStorageSync('Import_code')
```

### 查看当前路由
在控制台输入：
```javascript
const pages = Taro.getCurrentPages()
const currentPage = pages[pages.length - 1]
console.log(currentPage.route)
```

## 成功标志

✅ 未登录用户默认进入登录页
✅ 已登录用户默认进入 DIY 页面
✅ 未登录用户访问受保护页面时被拦截
✅ 登录成功后自动跳转到 DIY 页面
✅ 操作时会检查登录状态

全部通过即表示登录功能正常！
