# 登录流程完善说明

## 功能概述

已完善的登录逻辑包括：

### 1. 根据登录状态设置默认首页
- **未登录用户**：默认进入 `pages/profile/index`（个人中心/登录页）
- **已登录用户**：默认进入 `pages/diy/index`（DIY设计页）

### 2. 页面访问权限控制
- 未登录用户访问受保护页面时，会自动跳转到登录页并提示"请先登录"
- 受保护的页面包括：
  - DIY设计页 (`pages/diy/index`)
  - 购物车页 (`pages/cart/index`)
  - 我的设计页 (`pages/designs/index`)
  - 订单列表页 (`pages/order/list/index`)
  - 订单详情页 (`pages/order/detail/index`)
  - 订单确认页 (`pages/order/confirm/index`)

### 3. 登录成功后自动跳转
- 用户在个人中心页面完成登录后，会自动跳转到 DIY 设计页

## 实现细节

### 修改的文件

1. **src/app.config.ts**
   - 将 `pages/profile/index` 设置为第一个页面（默认首页）
   - 这样未登录用户会默认进入登录页

2. **src/app.ts**
   - 在 `useLaunch` 中添加启动时的登录状态检查
   - 在 `useDidShow` 中添加全局路由守卫
   - 自动根据登录状态跳转到合适的页面

3. **src/utils/authGuard.ts**（新建）
   - `checkLoginAndPrompt()`: 检查登录状态并显示提示对话框
   - `checkLoginForPage()`: 页面级别的登录守卫
   - `showLoginPrompt()`: 显示登录提示对话框
   - `navigateToProfile()`: 导航到登录页

4. **src/pages/profile/index.tsx**
   - 登录成功后自动跳转到 DIY 页面

## 测试步骤

### 测试场景 1：未登录用户启动应用
1. 清除应用数据（退出登录）
2. 重新启动小程序
3. **预期结果**：自动进入个人中心页面（登录页）

### 测试场景 2：已登录用户启动应用
1. 确保已登录
2. 重新启动小程序
3. **预期结果**：自动进入 DIY 设计页面

### 测试场景 3：未登录用户点击 TabBar
1. 清除应用数据（退出登录）
2. 点击底部 TabBar 的"DIY设计"或"购物车"
3. **预期结果**：
   - 显示提示"请先登录"
   - 自动跳转到个人中心页面

### 测试场景 4：未登录用户访问其他页面
1. 清除应用数据（退出登录）
2. 尝试通过其他方式访问受保护页面（如订单列表）
3. **预期结果**：
   - 显示提示"请先登录"
   - 自动跳转到个人中心页面

### 测试场景 5：登录成功后自动跳转
1. 在个人中心页面完成登录流程
2. **预期结果**：
   - 显示"登录成功"提示
   - 2秒后自动跳转到 DIY 设计页面

### 测试场景 6：操作时的登录检查
1. 清除应用数据（退出登录）
2. 在 DIY 页面尝试"加入购物车"或"保存设计"
3. **预期结果**：
   - 显示对话框："加入购物车需要先登录，是否前往登录？"
   - 点击"去登录"跳转到个人中心页面

## 技术实现

### 全局路由守卫
在 `src/app.ts` 中使用 `useDidShow` 钩子实现全局路由守卫：
- 监听每次页面显示
- 检查当前页面是否需要登录
- 未登录时自动跳转到登录页

### 操作级别的登录检查
在需要登录的操作中使用 `checkLoginAndPrompt()` 函数：
```typescript
const isLoggedIn = await checkLoginAndPrompt('加入购物车')
if (!isLoggedIn) {
  return // 用户未登录，不执行后续操作
}
```

### 登录状态判断
使用 `authService.isLoggedIn()` 检查本地存储中是否有有效的 token。

## 注意事项

1. **TabBar 页面跳转**：使用 `Taro.switchTab()` 而不是 `Taro.navigateTo()`
2. **延迟跳转**：在显示提示后延迟 500ms 跳转，确保用户能看到提示信息
3. **登录成功跳转**：在登录成功提示显示 2 秒后跳转，提升用户体验
4. **错误处理**：如果 `switchTab` 失败，会自动尝试使用 `reLaunch`

## 后续优化建议

1. 可以添加"记住我"功能，延长登录有效期
2. 可以添加自动刷新 token 的机制
3. 可以优化跳转动画和过渡效果
4. 可以添加更详细的日志记录，便于调试
