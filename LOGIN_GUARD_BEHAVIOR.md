# 登录守卫行为说明

## 功能概述

当未登录用户尝试访问需要登录的页面时，会显示对话框提示用户去登录。

## 受保护的页面

以下页面需要登录才能访问：
- DIY设计页 (`pages/diy/index`)
- 购物车页 (`pages/cart/index`)
- 我的设计页 (`pages/designs/index`)
- 订单列表页 (`pages/order/list/index`)
- 订单详情页 (`pages/order/detail/index`)
- 订单确认页 (`pages/order/confirm/index`)

## 用户交互流程

### 场景 1：未登录用户点击 TabBar
```
用户操作：点击"DIY设计"或"购物车"
↓
系统检测：用户未登录
↓
显示对话框：
  标题："需要登录"
  内容："访问此页面需要先登录，是否前往登录？"
  按钮：[取消] [去登录]
↓
用户选择：
  - 点击"去登录" → 跳转到个人中心页面
  - 点击"取消" → 返回上一页（如果有）或跳转到个人中心
```

### 场景 2：未登录用户通过其他方式访问受保护页面
```
用户操作：通过链接、分享等方式访问受保护页面
↓
系统检测：用户未登录
↓
显示对话框：（同上）
↓
用户选择：（同上）
```

## 技术实现

### 全局路由守卫
在 `src/app.ts` 的 `useDidShow` 钩子中实现：

```typescript
useDidShow(() => {
  // 获取当前路由
  const currentRoute = getCurrentRoute()
  
  // 检查是否需要登录
  if (isProtectedPage(currentRoute)) {
    if (!isLoggedIn()) {
      // 显示登录提示对话框
      showLoginModal()
    }
  }
})
```

### 对话框处理逻辑

**用户点击"去登录"：**
- 使用 `Taro.switchTab()` 跳转到个人中心页面
- 如果失败，降级使用 `Taro.reLaunch()`

**用户点击"取消"：**
- 检查页面栈深度
- 如果有上一页：使用 `Taro.navigateBack()` 返回
- 如果没有上一页：使用 `Taro.switchTab()` 跳转到个人中心

## 用户体验优化

### 1. 友好的提示信息
- 明确告知用户需要登录
- 提供"去登录"和"取消"两个选项
- 让用户有选择的权利

### 2. 智能的返回逻辑
- 用户取消时，优先返回上一页
- 如果没有上一页，跳转到个人中心
- 避免用户陷入无法操作的状态

### 3. 无缝的跳转体验
- 使用 `switchTab` 跳转 TabBar 页面
- 失败时自动降级使用 `reLaunch`
- 确保跳转一定能成功

## 测试要点

### 测试 1：TabBar 页面访问
1. 退出登录
2. 点击"DIY设计"
3. 验证对话框显示
4. 点击"去登录"，验证跳转到个人中心

### 测试 2：取消操作
1. 退出登录
2. 点击"购物车"
3. 点击"取消"
4. 验证返回到上一页或个人中心

### 测试 3：直接访问
1. 退出登录
2. 通过分享链接访问订单页面
3. 验证对话框显示
4. 验证跳转逻辑

## 与操作级别登录检查的区别

### 页面级别（全局路由守卫）
- 在页面显示时检查
- 拦截整个页面的访问
- 用户无法看到页面内容

### 操作级别（authGuard.ts）
- 在执行操作时检查
- 用户可以看到页面
- 只在执行特定操作时提示

### 使用场景对比

**页面级别适用于：**
- 整个页面都需要登录才能查看
- 如：订单列表、我的设计

**操作级别适用于：**
- 页面可以浏览，但操作需要登录
- 如：DIY 页面可以设计，但加入购物车需要登录

## 注意事项

1. **对话框不会重复显示**
   - 用户点击取消后，不会再次弹出
   - 除非用户重新尝试访问

2. **TabBar 页面跳转**
   - 必须使用 `switchTab` 而不是 `navigateTo`
   - TabBar 页面之间的跳转有特殊限制

3. **页面栈管理**
   - 注意页面栈的深度
   - 避免页面栈过深导致的问题

4. **用户体验**
   - 给用户选择的权利
   - 不要强制跳转
   - 提供清晰的提示信息
